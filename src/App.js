export default class App {

    /**
     * init - Initialize the app
     * @return {void} Not meant to return
     */
    static init() {
        console.log( 'Welcome to the Kindness' );
    }

}

App.init();
